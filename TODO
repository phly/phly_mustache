API documentation
Tests 
    Pragmas are section-specific
    Pragmas should only apply to the current template (not partials)
    for recursive sections and partials
    tokenization
End-user docs (particularly for extending via pragmas)
Whitespace handling
    If a section start and/or end is on a line by itself, don't add whitespace (including newlines)
